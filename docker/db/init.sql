-- データベース初期化スクリプト
-- このファイルはPostgreSQLコンテナの初回起動時に実行されます

-- データベースが既に存在する場合はスキップ
-- （このスクリプトは初期化時のみ実行されます）

-- タイムゾーンの設定
SET timezone = 'Asia/Tokyo';

-- 拡張機能の有効化（UUID生成用）
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- ここにテーブル作成などの初期化処理を追加できます
-- 実際のテーブル定義はGoのマイグレーションで管理することを推奨します
